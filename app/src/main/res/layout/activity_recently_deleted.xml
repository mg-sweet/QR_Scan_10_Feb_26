<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- ✅ 1. Boxed Toolbar with Expandable Search -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/toolbarCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardElevation="4dp"
        app:cardCornerRadius="0dp"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Notch Padding တွက်ချက်ရန် Container -->
        <FrameLayout
            android:id="@+id/toolbarContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            >

            <!-- 🟢 Normal Toolbar State -->
            <LinearLayout
                android:id="@+id/layoutNormalToolbar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingHorizontal="8dp"
                android:paddingVertical="12dp">

                <ImageButton
                    android:id="@+id/btnBack"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_back"
                    app:tint="@color/on_surface" /> <!-- ✅ Changed -->

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:layout_weight="1"
                    android:text="Recently Deleted"
                    android:textColor="@color/on_surface"
                    android:textSize="20sp"
                    android:textStyle="bold" />

                <!-- 🔍 Search Icon -->
                <ImageButton
                    android:id="@+id/btnOpenSearch"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:layout_marginEnd="4dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@android:drawable/ic_menu_search"
                    app:tint="#64748B" />

                <!-- Select Button for Selection Mode -->
                <TextView
                    android:id="@+id/btnEnterSelection"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="8dp"
                    android:background="@drawable/bg_icon_rounded"
                    android:paddingHorizontal="16dp"
                    android:paddingVertical="8dp"
                    android:text="Select"
                    android:textColor="#4F46E5"
                    android:textSize="16sp"
                    android:textStyle="bold" />
            </LinearLayout>

            <!-- 🔵 Search Toolbar State (Hidden by default) -->
            <LinearLayout
                android:id="@+id/layoutSearchToolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingHorizontal="8dp"
                android:paddingVertical="12dp"
                android:visibility="gone">

                <!-- ⬅️ Search Mode ထဲမှ ထွက်သည့် Back ခလုတ် -->
                <ImageButton
                    android:id="@+id/btnCloseSearch"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_back"
                    app:tint="#64748B" />

                <EditText
                    android:id="@+id/etSearchTrash"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_marginStart="8dp"
                    android:layout_weight="1"
                    android:background="@null"
                    android:hint="Search trash items..."
                    android:imeOptions="actionSearch"
                    android:inputType="textNoSuggestions"
                    android:maxLines="1"
                    android:textColor="#1E293B"
                    android:textColorHint="#94A3B8"
                    android:textSize="16sp" />

                <!-- ❌ စာသားကို ရှင်းလင်းသည့် Clear (X) ခလုတ် -->
                <ImageButton
                    android:id="@+id/btnClearSearch"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@android:drawable/ic_menu_close_clear_cancel"
                    android:visibility="gone"
                    app:tint="#94A3B8" />
            </LinearLayout>

        </FrameLayout>
    </com.google.android.material.card.MaterialCardView>
    <!-- ✅ 2. Trash Hint Text -->
    <TextView
        android:id="@+id/tvTrashHint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="Items in Trash will be permanently deleted after 30 days."
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/toolbarCard"/>

    <!-- ✅ 3. RecyclerView (List) -->
    <!-- Ad container ပေါ်မထပ်စေရန် bottom ကို adContainer ထိသာ သတ်မှတ်ထားပါသည် -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvTrash"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:padding="8dp"
        android:paddingBottom="80dp"
        app:layout_constraintBottom_toTopOf="@id/adContainer"
        app:layout_constraintTop_toBottomOf="@id/tvTrashHint" />

    <!-- ✅ 4. Empty State -->
    <LinearLayout
        android:id="@+id/emptyState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/adContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbarCard">

        <ImageView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:alpha="0.3"
            android:src="@drawable/ic_folder_empty" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Trash is empty"
            android:textColor="#94A3B8"
            android:textSize="16sp" />
    </LinearLayout>

    <!-- ✅ 5. Selection Action Bar (Floating) -->
    <!-- ကြော်ငြာအပေါ်မှ လွတ်ကင်းစွာ ပေါ်လာမည်ဖြစ်သည် -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/selectionBarCard"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="16dp"
        android:visibility="gone"
        app:cardCornerRadius="16dp"
        app:cardElevation="12dp"
        app:strokeColor="#4F46E5"
        app:strokeWidth="1dp"
        app:layout_constraintBottom_toTopOf="@id/adContainer">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp">

            <ImageButton
                android:id="@+id/btnSelectAll"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_select_all" />

            <TextView
                android:id="@+id/tvSelectionCount"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_weight="1"
                android:text="0 Selected"
                android:textColor="#1E293B"
                android:textStyle="bold" />

            <!-- Restore Selected Button -->
            <ImageButton
                android:id="@+id/btnRestoreSelected"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_restore"
                app:tint="#10B981" />

            <!-- Delete Selected Button -->
            <ImageButton
                android:id="@+id/btnDeleteSelected"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginStart="8dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_delete"
                app:tint="#EF4444" />

            <!-- Close Selection Button -->
            <ImageButton
                android:id="@+id/btnCloseSelection"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginStart="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_close_clear_cancel" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- ✅ 6. Ads Container (Bottom) -->
    <!-- အောက်ခြေတွင် အမြဲကပ်နေမည်ဖြစ်သည် -->
    <FrameLayout
        android:id="@+id/adContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="55dp"
        android:background="#F1F5F9"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- တကယ့် ကြော်ငြာ Code ထည့်ရန် နေရာ -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Advertisement Banner"
            android:textColor="#94A3B8"
            android:textSize="12sp"
            android:textStyle="italic" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>